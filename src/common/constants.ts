const dbConfig = {
  dialect: 'postgres',
  host: 'localhost',
  port: 5432,
  username: 'postgres',
  password: 'restaurantesllaneros',
  database: 'unilapp',
  user: 'postgres',
};

module.exports.dbConfig = dbConfig;
