const dbConfig = {
  dialect: 'postgres',
  host: 'localhost',
  port: 5432,
  username: 'postgres',
  password: 'unilapp',
  database: 'unilapp',
  user: 'postgres',
};

module.exports.dbConfig = dbConfig;
